--[[ 
    PART 1/2: REALISTIC JIGGLE PHYSICS & CORE ENGINE (DELTA MOBILE)
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

local Settings = {
    BoobFeature = false,
    BoobStiffness = 12,
    BoobDamping = 0.15,
    BoobSize = 1.0,
    
    SilentAim = false,
    CameraLock = false,
    SmoothLock = false,
    Smoothness = 0.15,
    TargetPart = "Head",
    WallCheck = false,
    TeamCheck = false,
    FOV = 200,
    ShowFOV = true,
    
    ESP_Name = false,
    ESP_Box = false,
    ESP_Health = false,
    ESP_Tracers = false,
    
    SpeedHack = false,
    WalkSpeedValue = 16,
    JumpHack = false,
    JumpPowerValue = 50,
    InfiniteJump = false
}

--------------------------------------------------
-- REALISTIC SOFT BOOB SYSTEM (REBUILT FOR MOBILE)
--------------------------------------------------
local CreatedParts = {}
local LeftMeshWeld, RightMeshWeld = nil, nil
local BaseLeftC0 = CFrame.new(-0.42, 0.1, -0.52)
local BaseRightC0 = CFrame.new(0.42, 0.1, -0.52)

local PhysPos = Vector3.new(0,0,0)
local PhysVel = Vector3.new(0,0,0)
local LastHRPPos = nil

local function DestroyBoobs()
    for _, v in ipairs(CreatedParts) do
        if v and v.Parent then pcall(function() v:Destroy() end) end
    end
    CreatedParts = {}
    LeftMeshWeld, RightMeshWeld = nil, nil
end

local function BuildSoftBoob(char)
    DestroyBoobs()
    if not char or not char.Parent then return end
    
    local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")
    local head = char:FindFirstChild("Head")
    if not torso then return end
    
    local skinCol = head and head.Color or torso.Color
    local pink1 = Color3.fromRGB(255, 130, 155)
    local pink2 = Color3.fromRGB(255, 80, 120)
    local s = Settings.BoobSize

    local function createSphere(cf, size, col, parentWeld)
        local p = Instance.new("Part")
        p.Shape = Enum.PartType.Ball
        p.Size = size
        p.Color = col
        p.Material = Enum.Material.SmoothPlastic
        p.CanCollide = false
        p.Massless = true
        p.Parent = char

        local w = Instance.new("Weld")
        w.Part0 = parentWeld or torso
        w.Part1 = p
        w.C0 = cf
        w.Parent = p
        
        table.insert(CreatedParts, p)
        return p, w
    end

    -- โครงสร้างทรงคล้อยหยดน้ำ (ไม่แข็งเป็นก้อนกลม)
    local lMain, lW = createSphere(BaseLeftC0, Vector3.new(1.05 * s, 1.25 * s, 1.15 * s), skinCol)
    local rMain, rW = createSphere(BaseRightC0, Vector3.new(1.05 * s, 1.25 * s, 1.15 * s), skinCol)
    LeftMeshWeld = lW
    RightMeshWeld = rW

    -- เสริมฐานล่างให้นุ่มมน
    createSphere(CFrame.new(0, -0.18 * s, 0.08 * s), Vector3.new(0.95 * s, 0.95 * s, 1.05 * s), skinCol, lMain)
    createSphere(CFrame.new(0, -0.18 * s, 0.08 * s), Vector3.new(0.95 * s, 0.95 * s, 1.05 * s), skinCol, rMain)

    -- ปานนมและหัวนม
    createSphere(CFrame.new(0, -0.02 * s, -0.55 * s), Vector3.new(0.38 * s, 0.38 * s, 0.08 * s), pink1, lMain)
    createSphere(CFrame.new(0, -0.02 * s, -0.55 * s), Vector3.new(0.38 * s, 0.38 * s, 0.08 * s), pink1, rMain)
    createSphere(CFrame.new(0, -0.02 * s, -0.58 * s), Vector3.new(0.15 * s, 0.15 * s, 0.1 * s), pink2, lMain)
    createSphere(CFrame.new(0, -0.02 * s, -0.58 * s), Vector3.new(0.15 * s, 0.15 * s, 0.1 * s), pink2, rMain)
end

-- คำนวณฟิสิกส์เด้งละมุนตามการเคลื่อนไหว
RunService.Heartbeat:Connect(function(dt)
    if Settings.BoobFeature and LocalPlayer.Character then
        local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp and LeftMeshWeld and RightMeshWeld then
            local pos = hrp.Position
            if LastHRPPos then
                local delta = (pos - LastHRPPos)
                local moveVel = hrp.CFrame:VectorToObjectSpace(delta / dt)
                
                local targetOff = Vector3.new(
                    math.clamp(-moveVel.X * 0.02, -0.4, 0.4),
                    math.clamp(-moveVel.Y * 0.03, -0.5, 0.5),
                    math.clamp(moveVel.Z * 0.02, -0.3, 0.3)
                )

                local force = (targetOff - PhysPos) * Settings.BoobStiffness
                PhysVel = (PhysVel + force * dt) * (1 - math.clamp(Settings.BoobDamping, 0, 1))
                PhysPos = PhysPos + PhysVel * dt
            end
            LastHRPPos = pos

            LeftMeshWeld.C0 = BaseLeftC0 * CFrame.new(PhysPos) * CFrame.Angles(PhysPos.Y * 0.9, -PhysPos.X * 0.8, PhysPos.X * 0.4)
            RightMeshWeld.C0 = BaseRightC0 * CFrame.new(PhysPos) * CFrame.Angles(PhysPos.Y * 0.9, PhysPos.X * 0.8, -PhysPos.X * 0.4)
        end
    else
        LastHRPPos = nil
    end
ennil--[[ 
    PART 2/2: WIND UI & CONTROLS
]]

local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "WindUI | ระบบล็อคและนมละมุน (Delta Fixed)",
    Folder = "WindUI_DeltaFix",
    Size = UDim2.fromOffset(560, 440),
    Transparent = true,
    Theme = "Dark"
})

-- หน้าปรับนมละมุน
local BoobTab = Window:Tab({ Title = "ฟิสิกส์นมเด้ง", Icon = "user" })
BoobTab:Toggle({ 
    Title = "เปิดใช้งานระบบนมเด้ง", 
    Default = Settings.BoobFeature, 
    Callback = function(v)
        Settings.BoobFeature = v
        if v then 
            BuildSoftBoob(LocalPlayer.Character) 
        else 
            DestroyBoobs() 
        end
    end 
})

BoobTab:Slider({ 
    Title = "ขนาดหน้าอก (อัปเดตทันที)", 
    Min = 0.5, 
    Max = 2.5, 
    Default = Settings.BoobSize, 
    Callback = function(v)
        Settings.BoobSize = v
        if Settings.BoobFeature and LocalPlayer.Character then
            BuildSoftBoob(LocalPlayer.Character)
        end
    end 
})

BoobTab:Slider({ 
    Title = "ความเด้งนุ่มนวล", 
    Min = 5, 
    Max = 25, 
    Default = Settings.BoobStiffness, 
    Callback = function(v)
        Settings.BoobStiffness = v
    end 
})

-- หน้าการเคลื่อนไหว
local MovementTab = Window:Tab({ Title = "การเคลื่อนไหว", Icon = "run" })
MovementTab:Toggle({ Title = "เปิดใช้ Speed Hack", Default = Settings.SpeedHack, Callback = function(v) Settings.SpeedHack = v end })
MovementTab:Slider({ Title = "ความเร็วเดิน", Min = 16, Max = 250, Default = Settings.WalkSpeedValue, Callback = function(v) 
    Settings.WalkSpeedValue = v 
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = v
    end
end })

LocalPlayer.CharacterAdded:Connect(function(char)
    task.wait(1)
    if Settings.BoobFeature then BuildSoftBoob(char) end
end)

WindUI:Notify({ Title = "เรียบร้อย!", Content = "แก้ไขระบบการอัปเดตขนาดและฟิสิกส์บน Delta แล้ว", Duration = 4 })
