--[[ 
    WindUI | SILENT AIM + ESP (AIM COLOR)
    + RAINBOW OUTLINE ESP (IMAGE STYLE)
    + CAMERA LOCK MODE (NO BULLET REDIRECT)
    AUTO HYBRID HEAD LOCK
    ONE FILE FINAL | AUTO CLEAN
    + UTILITY & BOOST EXTENSIONS (OPTIMIZED)
    + HITBOX EXPANDER
    + TROLL BOOB PHYSICS & ARCEUS FLY
]]

--------------------------------------------------
-- Load WindUI
--------------------------------------------------
local WindUI
do
    local ok, result = pcall(function()
        return require("./src/Init")
    end)
    WindUI = ok and result or loadstring(game:HttpGet(
        "https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"
    ))()
end

--------------------------------------------------
-- Services
--------------------------------------------------
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

--------------------------------------------------
-- States (Tab 1 Original - UNTOUCHED)
--------------------------------------------------
local SilentAim   = false
local CAMERA_LOCK = false
local ESP_ENABLED = false
local RAINBOW_ESP = false

local TeamCheck  = true
local MaxDistance = 600
local FOV = 120

local CurrentTarget = nil
local ESP_CACHE = {}
local HIGHLIGHT_CACHE = {}

--------------------------------------------------
-- States (Optimized Extensions)
--------------------------------------------------
local WalkSpeedVal = 16
local WalkSpeedEnabled = false
local InfiniteJumpEnabled = false

local BrightnessVal = 1
local FullbrightEnabled = false
local OriginalBrightness = Lighting.Brightness
local OriginalClockTime = Lighting.ClockTime

local ESP_NPC_ENABLED = false
local NPC_CACHE = {}

-- Hitbox States
local HitboxEnabled = false
local HitboxSize = 10
local OriginalSizes = {}

--------------------------------------------------
-- States (Boob Physics System)
--------------------------------------------------
local BoobSettings = {
    Enabled = false,
    Stiffness = 12,
    Damping = 0.15,
    Size = 1.0
}

local CreatedBoobParts = {}
local LeftMeshWeld, RightMeshWeld = nil, nil
local BaseLeftC0 = CFrame.new(-0.42, 0.1, -0.52)
local BaseRightC0 = CFrame.new(0.42, 0.1, -0.52)

local PhysPos = Vector3.new(0,0,0)
local PhysVel = Vector3.new(0,0,0)
local LastHRPPos = nil

--------------------------------------------------
-- Utility (Original)
--------------------------------------------------
local function isAlive(char)
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    return hum and hum.Health > 0
end

local function isTeammate(p)
    if not TeamCheck then return false end
    if not LocalPlayer.Team or not p.Team then return false end
    return p.Team == LocalPlayer.Team
end

local function rainbowColor(speed)
    local t = tick() * speed
    return Color3.fromHSV(t % 1, 1, 1)
end

local function isCharValid(char)
    return char and char.Parent and char:IsDescendantOf(workspace)
end

--------------------------------------------------
-- Soft Boob Physics Mechanics
--------------------------------------------------
local function DestroyBoobs()
    for _, v in ipairs(CreatedBoobParts) do
        if v and v.Parent then pcall(function() v:Destroy() end) end
    end
    CreatedBoobParts = {}
    LeftMeshWeld, RightMeshWeld = nil, nil
end

local function BuildSoftBoob(char)
    DestroyBoobs()
    if not char or not char.Parent then return end
    
    local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")
    local head = char:FindFirstChild("Head")
    if not torso then return end
    
    local skinCol = head and head.Color or torso.Color
    local pink1 = Color3.fromRGB(255, 130, 155)
    local pink2 = Color3.fromRGB(255, 80, 120)
    local s = BoobSettings.Size

    local function createSphere(cf, size, col, parentWeld)
        local p = Instance.new("Part")
        p.Shape = Enum.PartType.Ball
        p.Size = size
        p.Color = col
        p.Material = Enum.Material.SmoothPlastic
        p.CanCollide = false
        p.Massless = true
        p.Parent = char

        local w = Instance.new("Weld")
        w.Part0 = parentWeld or torso
        w.Part1 = p
        w.C0 = cf
        w.Parent = p
        
        table.insert(CreatedBoobParts, p)
        return p, w
    end

    local lMain, lW = createSphere(BaseLeftC0, Vector3.new(1.05 * s, 1.25 * s, 1.15 * s), skinCol)
    local rMain, rW = createSphere(BaseRightC0, Vector3.new(1.05 * s, 1.25 * s, 1.15 * s), skinCol)
    LeftMeshWeld = lW
    RightMeshWeld = rW

    createSphere(CFrame.new(0, -0.18 * s, 0.08 * s), Vector3.new(0.95 * s, 0.95 * s, 1.05 * s), skinCol, lMain)
    createSphere(CFrame.new(0, -0.18 * s, 0.08 * s), Vector3.new(0.95 * s, 0.95 * s, 1.05 * s), skinCol, rMain)

    createSphere(CFrame.new(0, -0.02 * s, -0.55 * s), Vector3.new(0.38 * s, 0.38 * s, 0.08 * s), pink1, lMain)
    createSphere(CFrame.new(0, -0.02 * s, -0.55 * s), Vector3.new(0.38 * s, 0.38 * s, 0.08 * s), pink1, rMain)
    createSphere(CFrame.new(0, -0.02 * s, -0.58 * s), Vector3.new(0.15 * s, 0.15 * s, 0.1 * s), pink2, lMain)
true--[[ 
    [PART 2/2] UI INTERFACE & WINDOW CONTROL
]]

--------------------------------------------------
-- UI Window Initialization
--------------------------------------------------
local Window = WindUI:CreateWindow({
    Title="Thug Hunters💢👿",
    Folder="WindHub",
    OpenButton={Title="Thug Hunters💢👿",Enabled=true}
})

--------------------------------------------------
-- TAB 1: ORIGINAL MAIN MENU
--------------------------------------------------
local Main = Window:Tab({Title="เมนู(กด)"})

Main:Toggle({Title="🔫ล็อค(บังคับกระสุน)", Callback=function(v) SilentAim=v end})
Main:Toggle({Title="🔫ล็อค(หันจอไปหัว)", Callback=function(v) CAMERA_LOCK=v end})
Main:Toggle({Title="👑ESP(ชื่อ)", Callback=function(v) ESP_ENABLED=v end})
Main:Toggle({Title="👹ESP(กรอบสีรุ้ง)", Callback=function(v) RAINBOW_ESP=v end})
Main:Toggle({Title="Team Check", Default=true, Callback=function(v) TeamCheck=v end})
Main:Slider({Title="👹FOV", Value={Min=40,Max=4000,Default=FOV}, Callback=function(v) FOV=v end})
Main:Slider({Title="👹ระยะ", Value={Min=100,Max=12000,Default=MaxDistance}, Callback=function(v) MaxDistance=v end})

--------------------------------------------------
-- TAB 2: PLAYER OPTIONS
--------------------------------------------------
local PlayerTab = Window:Tab({Title="Player Options"})

PlayerTab:Toggle({Title="เปิดใช้งานการวิ่งเร็ว", Callback=function(v) 
    WalkSpeedEnabled = v 
    if not v and LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = 16 end
    end
end})
PlayerTab:Slider({Title="ปรับความเร็วการวิ่ง", Value={Min=16, Max=250, Default=WalkSpeedVal}, Callback=function(v) WalkSpeedVal = v end})

PlayerTab:Button({Title="🚀 เปิดใช้งานการบิน (Arceus Fly)", Callback=function()
    pcall(function()
        loadstring("\108\111\97\100\115\6\114\105\110\103\40\103\97\109\101\58\72\116\116\112\71\101\116\40\40\39\104\116\116\112\115\58\47\47\103\105\115\116\46\103\105\115\116\104\117\98\117\115\101\114\99\111\110\116\101\110\116\46\99\111\109\47\109\101\111\122\111\110\101\89\84\47\98\102\48\51\55\100\102\102\57\102\48\97\55\48\48\49\55\51\48\52\100\100\100\54\55\102\100\99\100\51\55\48\47\114\97\119\47\101\49\52\101\55\52\102\52\50\53\98\48\54\48\100\102\53\50\51\51\52\51\99\102\51\48\98\55\56\55\48\55\52\101\98\51\99\53\100\50\47\97\114\99\101\117\115\37\50\53\50\48\120\37\50\53\50\48\102\108\121\37\50\53\50\48\50\37\50\53\50\48\111\98\102\117\115\99\97\116\111\114\39\41\44\116\114\117\101\41\41\40\41\10\10")()
    end)
end})

PlayerTab:Toggle({Title="กระโดดไม่จำกัด (Infinite Jump)", Callback=function(v) InfiniteJumpEnabled = v end})

PlayerTab:Toggle({Title="เปิดใช้งาน Hitbox (ขยายตัวศัตรู)", Callback=function(v) 
    HitboxEnabled = v 
    UpdateHitbox()
end})
PlayerTab:Slider({Title="ปรับขนาด Hitbox", Value={Min=2, Max=50, Default=HitboxSize}, Callback=function(v) 
    HitboxSize = v 
    if HitboxEnabled then UpdateHitbox() end
end})

--------------------------------------------------
-- TAB 3: VISUAL & WORLD
--------------------------------------------------
local VisualTab = Window:Tab({Title="Visual & World"})

VisualTab:Toggle({Title="เปิดความสว่างแมพ (Fullbright)", Callback=function(v) 
    FullbrightEnabled = v 
    if not v then
        Lighting.Brightness = OriginalBrightness
        Lighting.ClockTime = OriginalClockTime
    end
end})
VisualTab:Slider({Title="ระดับความสว่าง", Value={Min=1, Max=10, Default=BrightnessVal}, Callback=function(v) BrightnessVal = v end})

VisualTab:Toggle({Title="ESP มองเห็น NPC", Callback=function(v) ESP_NPC_ENABLED = v end})

VisualTab:Button({Title="🚀 อัปเดต FPS (Optimize/Boost FPS)", Callback=function()
    for _, item in ipairs(workspace:GetDescendants()) do
        if item:IsA("BasePart") then
            item.Material = Enum.Material.SmoothPlastic
        elseif item:IsA("Decal") or item:IsA("Texture") then
            item:Destroy()
        end
    end
    Lighting.GlobalShadows = false
end})

--------------------------------------------------
-- TAB 4: SERVER & MISC
--------------------------------------------------
local ServerTab = Window:Tab({Title="Server & Misc"})

ServerTab:Button({Title="🌐 เข้าร่วม Server คนน้อย (Rejoin Low Players)", Callback=function()
    joinLowPlayerServer()
end})

--------------------------------------------------
-- TAB 5: เกรียน (BOOB PHYSICS CONTROL)
--------------------------------------------------
local TrollTab = Window:Tab({Title="เกรียน"})

TrollTab:Toggle({
    Title = "🍑 เปิดใช้งานโหมดนมเด้งละมุน",
    Default = BoobSettings.Enabled,
    Callback = function(v)
        BoobSettings.Enabled = v
        if v then
            BuildSoftBoob(LocalPlayer.Character)
        else
            DestroyBoobs()
        end
    end
})

TrollTab:Slider({
    Title = "📐 ปรับขนาดหน้าอก (เรียลไทม์)",
    Value = {Min = 0.5, Max = 3.0, Default = BoobSettings.Size},
    Callback = function(v)
        BoobSettings.Size = v
        if BoobSettings.Enabled and LocalPlayer.Character then
            BuildSoftBoob(LocalPlayer.Character)
        end
    end
})

TrollTab:Slider({
    Title = "🌀 ปรับความนุ่มเด้ง (Physics Force)",
    Value = {Min = 5, Max = 30, Default = BoobSettings.Stiffness},
    Callback = function(v)
        BoobSettings.Stiffness = v
    end
})
